# Roadmap

- Add Windows authentication log support alongside Linux SSH `auth.log`
- Add optional IP enrichment such as basic geolocation or reputation context
- Expand configurable rules while keeping the CLI-first workflow simple
- Package the tool as an installable CLI utility with a cleaner entry command
