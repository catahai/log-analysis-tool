# Roadmap

- Add Windows authentication log support alongside Linux `auth.log`
- Add optional IP enrichment such as basic geolocation or reputation context
- Expand configurable rules while keeping the default CLI workflow simple
- Package the project as an installable CLI tool with a clean entry command
